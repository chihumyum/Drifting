# Run artifacts (run.json + rows.jsonl) are local/ephemeral; persistent trend
# tracking is the eval_run DB table (a later phase), not these files.
runs/

# Local book prose dropped here (corpus/books/<id>/*.md) — never committed.
books/
