# 前端架构与性能优化实施计划

更新：2026-09-13。审查基线：`8106f90b5ab898f51d296f6c2fb838e13dd74c2c`。

**状态：F0 持续补充基线；F1 输入路径已实现并通过自动验收，实机 IME 待测；F2 字段订阅及编辑器共享名称/外观索引已实现，已提交 generation 与请求代次已分离，SQLite/React 组合验收通过，完整应用验收待继续；F3 已实现独立编辑器会话、按可见性发布 outline、实体 Review 装饰与自动链接接线，以及打字机滚动、共享大纲几何/高亮控制器，审阅标记已按语义订阅并暂停隐藏几何；原生控制组及标记/语言切换连续性已验收，选区捕获已去除全文扫描，恢复焦点与关闭清理已验收；上下文菜单归属与隐藏清理已实现，格式/拆段的 Yjs 撤销和初始化延迟失焦缺陷已修复，原生组合及重启已验收；斜杠/提及菜单已实现可见命令归属与异步写入失效保护，浏览器及原生组合验收通过，并修复隐藏事务后菜单重现问题；内联 Copilot 已提取会话归属、清理旧上下文并隔离迟到响应，浏览器回归通过；内联改写应用已独立拆分并校验原始目标、拒绝部分应用，前后输入与改写已隔离撤销，浏览器/Yjs 及原生组合、正常退出/重启验收通过；Copilot 能力的准备、请求和摘要已按会话管理取消，异步竞态浏览器回归与原生组合回归已分别通过；其余显示/交互拆分及设备/性能预算待继续；F4 已实现私有事件去重索引、共享聊天显示通知合并，以及桌面/移动消息区域独立订阅与移动输出操作归属；两端浏览器滚动、输入隔离及移动异步操作已验收，移动组件的原生 WebKit 场景及组合/重启回归已通过，完整恢复与手机/设备预算待继续；F5 已实现关系索引、独立连线几何层、Timeline 预览隔离、稳定 Super Element / Story Graph 卡片树，独立的 Drift 拖动临时状态和未放置章节浮层，完整图谱及设备验收待继续；F6 已实现快照引用复用、引用队列和纯 Yjs 引用局部读取；结构读取按 SQLite 失效覆盖收窄至受影响集合及节点/元素行，renderer 进程恢复与原生迁移验收已实现，完整原生恢复与总体性能待继续；F7 已实现 PDF/ZIP、设置和图谱按需加载，以及有界入口预加载；浏览器重试、局部导航及合成草稿连续性已验收，其他重型入口、原生和总体预算待继续；F8 已建立完整桌面 App 控制组的组合与重启验收，并接入普通 CI 确定性浏览器回归通道，本地通过、托管执行待推送；综合压力、固定设备性能门槛和设备矩阵待继续。**
已发生的工作和测量边界见[执行记录](README.md)。原生控制组组合正确性已有验收；原生和全 App 性能预算仍未验收。
本文件描述后续改造，不改变当前产品契约。阶段、依赖和待验收项见
[`acceptance/optimization-plan.json`](acceptance/optimization-plan.json)。该文件由人工维护；
`evidence: []` 表示尚待验证，不能解释为检查通过、功能实现或实机验收。

## 1. 目标与判断依据

目标是让输入、标签切换、Agent 流式输出和图谱操作的开销，主要取决于本次变化涉及的内容，
并控制大型项目、长会话和多标签场景下的增长。模块拆分服务于数据归属、失效范围与独立验证。

当前源码中可定位的集中点如下。行数仅用于定位审查对象，不能直接证明卡顿。

| 对象 | 基线观察 | 优化价值与证据边界 |
| --- | --- | --- |
| `useEntityEditor.ts`，1,564 行；`entity-link.ts` | 文档变化会触发全文实体链接 DOM 查询和颜色写入；上下文颜色解析包含数组查找 | 位于编辑热路径；已确认重复操作路径，尚未测量真实输入延迟 |
| `data-store` 消费者、`EditorMainArea` | 审查定位 25 处无 selector 的 `useDataStore()` 调用；所有已打开的桌面标签保留编辑器；各编辑器又订阅多组项目数据 | 数据更新可传播到不相关和隐藏视图；保留编辑器本身承担连续性契约 |
| `agent-chat-store.ts`，1,451 行 | 每个 journal/transient 事件复制已见事件 ID 对象；累计复制量随事件数呈平方增长 | 长流式会话有明确算法风险；不能把隔离表达式的运行时间当作界面卡顿时长 |
| `DesktopSuperElementView.tsx`，3,069 行；`DesktopStoryGraphView.tsx`，2,351 行；`DesktopBottomTimeline.tsx`，1,710 行 | 数据投影、几何测量、手势和浮层集中；连线中查找实体，几何状态更新落在大组件 | 需要隔离更新范围，减少重复查找和布局读取；已有手势优化须保留 |
| `ProjectRuntimeProvider`、`workspace-projection.service`、`reference-index.service` | 结构变更走完整工作区捕获，发布后启动整项目正文引用重建 | 小变更可能触发大项目开销；正文增量已有独立路径，不能全部归为全量刷新 |
| `AppRoutes`、`DesktopOverlayHost` | 多个路由、图谱和浮层静态导入；该基线既有构建的主 JS 约 5.29 MB，gzip 约 1.48 MB | 是加载边界候选；文件字节数不能代替原生启动实测 |

既有优化应继续保留：正文持久化 400 ms 合并及退出/失焦/保存刷新、自动链接 500 ms 合并、
普通事务映射 dangling decorations、Agent 历史消息 memo、图谱平移中的直接 DOM transform、
正文同步与结构刷新分流，以及工作区的 `projectId + epoch` 原子发布。
具体参数以实施时源码为准，F0 需复核基线是否漂移。

## 2. 架构边界与目标分工

沿用[当前 renderer 架构](../renderer-ui-architecture.md)：desktop/mobile 是兄弟 shell，
共享项目运行时。增加明确的读取、计算和显示边界，不创建第二套正文或 Agent 权威状态。

| 层/职责 | 权威数据与生命周期 | 允许的失效和更新范围 |
| --- | --- | --- |
| 项目运行时 | SQLite、live Yjs、同步、Agent Runtime；按项目/用户生命周期持有 | 事务提交和项目切换；不随子路由或普通标签切换重启 |
| 项目只读快照与 selector | 一致性快照及 ID 查询；缓存归属项目和 generation | 同一批快照原子可见；只改变语义上受影响的记录与集合引用 |
| 派生索引 | 名称、颜色、归属、关系端点、图谱读模型 | 按语义修订失效；支持清空重建，无权覆盖 SQLite/Yjs |
| 编辑器会话 | 文档连接、撤销历史、选择、滚动和未提交字段 | 与打开标签生命周期一致；隐藏时仍接收权威更新和完成必要持久化 |
| 编辑器显示效果 | 链接着色、Review 装饰、浮层和可见统计 | 按实体、可见性及版本更新；显示前补齐，不重建文档会话 |
| Agent 投影 | 持久日志投影和 transient 展示状态 | 去重、顺序与恢复归运行时投影层；React 只订阅所需会话/消息 |
| 图谱视图 | 共享纯读模型；shell 各自拥有手势、导航和浮层 | 视口、几何、卡片、选择分别更新；不把移动端手势搬进共享数据层 |
| 路由和重型功能入口 | 模块加载与错误/重试呈现 | 按访问需求加载；不能因 lazy 边界重复创建项目运行时 |

实施时应在现有 `features`、`services`、`store`、`hooks` 下选择最窄归属。
无需为每行表格创建顶级目录、通用事件总线或新的全局 store。
先写清输入、输出、owner、dispose 与失效条件，再抽模块。

必须守住的边界：

- live Yjs 是正文真相；`contentJson` 是 seed/cache；自动正文写入继续经过 live `Y.Doc` 或 `writeChapterProse`。
- 隐藏标签不因性能优化丢失文档、撤销、滚动、选区或草稿。可见 split 的两侧都要正常显示，仅焦点侧拥有命令。
- 未审阅 Agent 内容不能因延迟装饰或标签切换短暂暴露；Review 决定仍持久化在 SQLite。
- 工作区不能混用两个项目、两个 generation 或半批新旧关系数据；旧异步结果不能发布到新项目。
- 不修改已发布迁移，不重置作者数据库。若 F6 确需新增持久进度结构，另加迁移并履行安全快照与兼容性验收。
- 不改变 provider-neutral Agent 的 `local`/`sidecar`/`remote` 接缝，也不恢复已退役产品。
- [Copilot 重设计](../copilot/redesign-plan.md)仍按独立计划推进；本计划只可提供订阅/调度基础，不能据此宣称其功能完成。
- 保留 drift 节点任一端点决定特殊边的规则，以及 act/chapter/scene/beat/note 五层语义。

## 3. 阶段与依赖

| 阶段 | 交付主题 | 前置条件 | 主要收益 | 风险 |
| --- | --- | --- | --- | --- |
| F0 | 可复现基线和验收入口 | 无 | 找准实际瓶颈，建立比较方法 | 低 |
| F1 | 输入热路径与链接着色 | F0 | 降低每次输入的重复查询和写入 | 中：IME、事务映射 |
| F2 | 精确订阅和共享项目索引 | F0 | 缩小状态变化的传播范围 | 中：缓存失效、一致性 |
| F3 | 编辑器会话与显示效果分层 | F1、F2 | 多标签成本不再直接乘以全部显示计算 | 中高：连续性与审阅遮罩 |
| F4 | Agent 日志与界面投影分层 | F0 | 降低长会话累计复制和通知成本 | 中高：顺序、去重、恢复 |
| F5 | 图谱读模型、几何与交互拆分 | F2 | 大图操作只更新受影响的层 | 中高：连线与手势正确性 |
| F6 | 工作区投影与引用索引增量化 | F2 | 小结构变化避免整项目派生重建 | 高：同步、崩溃恢复 |
| F7 | 按功能加载与启动优化 | F0、F3、F5 | 减少首屏实际解析和执行的代码 | 中：加载失败与首开体验 |
| F8 | 综合压力、实机验收和 CI 防回退 | F0–F7 | 确认组合收益和长期稳定性 | 中：设备覆盖 |

建议实施顺序：**F0 → F1 → F2 → F3 → F4 → F5 → F6 → F7 → F8**。
这表示默认优先级，表中前置条件才是硬依赖。F4 可在 F0 后独立推进；若启动实测成为首要问题，
可先做 F7 的加载分析和安全的独立页面分块，涉及 editor/graph 边界的部分仍等待 F3/F5。
F0 后根据输入、内存、同步和启动测量重新排序；数据正确性门槛不得因排序变化降低。

每阶段按“证据/复现 → 最小实现 → 回归和契约更新”拆成可审查的提交或 PR。
不要求凑固定提交数，也不把所有重构合成一次大合并。阶段完成必须同时具有代码、对应持久文档和真实验收证据。

## 4. F0：建立可复现的性能基线

**产出：** 合成项目生成入口、场景执行说明、运行结果格式和首份基线报告。
先复用 [Dev CLI](../dev-cli/README.md) 建立合成数据，使用
[frontend debug](../frontend-debug.md) 的只读检查面采集；不向运行中的个人项目注入数据。

拟定测试矩阵如下，所有规模均为待 F0 校准的候选，不能直接解释为承诺容量。

| 维度 | 候选规模 | 检查目的 |
| --- | --- | --- |
| 项目实体 | 50 / 300 / 1,000 章节；100 / 1,000 / 5,000 元素 | 区分跟随活动文档增长和跟随整个项目增长的成本 |
| 关系 | 500 / 5,000 / 30,000 条 | 图谱构建、端点查找、测量与视口密度 |
| 当前正文 | 5,000 / 20,000 / 50,000 中文字符；0 / 100 / 500 个实体链接 | 输入、粘贴、自动链接、撤销与着色 |
| 会话 | 1 / 5 / 20 个打开标签，包含 split | 切换、后台计算、恢复显示与内存释放 |
| Agent | 1,000 / 4,000 / 16,000 个事件，外加重复、重放、取消和长历史 | 去重复杂度、投影合并与恢复 |
| 同步 | 单实体结构变化、小批结构变化、纯正文、整项目恢复 | 更新范围、原子性与引用追赶 |

不用做所有维度的笛卡尔积。先设小项目控制组，再单独放大一个维度，最后组合关键压力场景。
所有正文必须合成，不提交个人作品、数据库、绝对个人路径或账号信息。

采集至少覆盖：输入到绘制 p50/p95、主线程长任务、组件 commit 数/耗时、链接查询/解析/写入次数、
Agent 去重操作及通知次数、端点测量次数、SQL/索引处理源数、打开与关闭标签后的内存、首屏实际加载/执行模块。
内存报告区分 JS heap 与原生进程，并说明 GC 和采样条件。

记录 source SHA、构建类型、应用包身份、设备/内存、系统、WebView、fixture seed/hash、操作序列、
预热和重复次数。耗时比较采用同环境多次运行的中位数和 p95；微基准与端到端测量分开。
测原生应用前，确认启动的是对应 checkout 的新构建。CDP、模拟器合成输入不替代实机中文输入法或触摸验收。

现有[桌面 RC 性能目标](../qa/desktop-alpha-release-candidate.md)是 M1/8GB 上冷启动可交互 ≤4 s、
打开 5 万中文字符 ≤3 s、输入到绘制 p95 ≤50 ms；这是待验收目标。
其他场景的时间/内存预算在 F0 用基线与设备能力校准，并在实现前固定，不能事后挪动门槛来制造收益。
不把“文件小了多少”或开发构建中的单次耗时作为性能通过标准。

**验收 F0-01–04：** 可重新生成同一合成数据；可关联构建和结果；能分辨全量/局部更新；预算、
设备及未运行项明确。性能采集入口不增加生产默认热路径成本，不收集正文内容。

**回退：** 关闭或移除采集器，不触碰业务数据；基线不可信时先修复测量，不据此启动大范围改造。

## 5. F1：先修输入热路径

**范围：** `lib/extensions/entity-link.ts`、`lib/entity-link-appearance.ts`、`hooks/useEntityEditor.ts` 中的着色接线。

1. 将目标 ID 到颜色/归属的解析建立为可复用查询，避免每个链接都遍历项目数组。
   F1 可先提供局部接口；F2 再归并到项目索引，避免同时维护两套失效规则。
2. 先加入颜色未变化时不写 style 的保护，再跟踪事务影响的链接范围。
   普通文字编辑只处理新建或受影响的链接 DOM；纯 selection 事务不扫描全文。
3. 处理 mark 跨边界、粘贴、删除、undo/redo、远程 Yjs 变化及 ProseMirror 重建 DOM 的情况。
   优先使用事务映射/插件状态确定影响范围；不可只凭输入字符或旧 DOM 身份跳过更新。
4. 名称、归属、主题或颜色设置变化使用明确的 appearance revision；必要的全量重着色保留为可测量的慢路径。
   初次挂载、恢复和无法安全定位范围时也允许显式全量处理。
5. 保留自动链接、持久化合并和已有 decoration 映射。首轮不改正文模型或 Yjs 写入时序。

**验收 F1-01–04：** 长文中在链接外连续输入，不逐键查询所有链接；selection-only 更新不触发着色扫描；
颜色未变时无冗余 style 写入；新链接、颜色/归属变化、粘贴、撤销、远程编辑和中文组合输入显示正确。
在同一 fixture 下比较输入分位数和长任务，达到 F0 固定预算。

**回退：** 可单独恢复旧着色策略；缓存可丢弃重建，正文不变。出现链接遗漏或组合输入异常，
先回退范围优化，再保留已经独立验证的查询索引和 no-op 保护。

## 6. F2：收窄订阅，建立项目派生索引

**范围：** `store/data-store.ts` 及高频消费者、`useEntityEditor` 的名称/颜色索引、项目统计投影。

1. 先处理 NodeEditor、左右栏、BottomTimeline、图谱等热路径的全 store 订阅，改为字段/实体 selector。
   action 单独订阅；返回对象/数组的 selector 必须有稳定结果或合适的相等判断。
2. 给项目只读层建立按 ID 的查询与稳定记录引用；一次快照仍原子发布。
   不在各组件内各建一份 `Map`，也不因一次统计字段改变重建所有实体名称/颜色缓存。
3. 明确至少四类失效：实体存在/名称、归属/颜色、正文指标、关系/排序。
   修订号从真实语义变更导出；不能漏标、依赖调用者随手递增，或在 render 中全量拼字符串。
4. 名称解析、链接外观、关系端点查找归属项目级派生缓存；按 project/generation 清理。
   暴露只读查询，不让消费者修改共享 Map。旧项目异步计算完成后也不能污染新项目。
5. 写作统计仅因所需指标变化重算。当前总字数相同时已有 no-op，应保留，不将其误写为每次都会持久化。

**验收 F2-01–04：** comment 更新不触发仅依赖章节/名称/颜色的消费者；单节点指标更新不使名称/外观索引失效；
相关更名、删除、归属和排序可立即正确失效；换项目、刷新和恢复没有混合快照或残留缓存。
用订阅通知和实际 React commit 验证，不能只统计 `useDataStore()` 文本出现次数。

F2c 已将读取请求 epoch 与已提交数据 generation 分离。普通刷新和失败重试不改变
名称投影身份；真实 SQLite generation 变更在整批提交时更换行、成员关系和名称缓存。
同一 8 项 SQLite 检查从基线失败 7 项变为全部通过，连同既有回归共 38 项；1/5/20 组
React 消费者的 100 次评论/指标刷新，名称提交及自动链接目标重建由 100/500/2,000 次
分别降至 0。组合检查覆盖更名、归属颜色、缺失清空、generation 竞争和 A→B→A，
已接入确定性浏览器 CI 校验。见 `acceptance/f2-projection-generation.json`；这不代表
完整 ProjectRuntimeProvider/编辑器、原生 WebKit 或设备预算已经验收，F2 保持进行中。

**回退：** 按消费者回退 selector 接线或清空派生缓存，不更换权威 store。
第一轮无需把全部历史数组 API 一次性迁到新结构，也不增加数据库表。

## 7. F3：保留编辑会话，暂停不可见的显示工作

**范围：** `EditorMainArea`、`EditorSurfaceLifecycle`、`useEntityEditor` 及 Agent Review decoration 接线。

1. 将 `useEntityEditor` 按生命周期拆成文档/持久化、链接索引接线、Review 装饰、可见交互等有明确 owner 的单元。
   对外入口仍可保留；禁止只是把所有 hook 搬到新文件后在每个编辑器中全部运行。
2. Review 订阅按实体和实际相关修订过滤，避免每个编辑器对所有 Agent edit store 变化 dispatch meta 事务。
   同实体的多标签/多 split 仍共享权威状态，分别保持显示正确。
3. 隐藏会话保留 Yjs、必要持久化、撤销和字段草稿；可延迟纯显示的全量装饰/测量，将其标为 dirty。
   文档变化仍须满足编辑器及审阅安全要求，不能一概暂停所有插件或远程更新。
4. 在已有 ready/可见提交屏障中补齐 dirty 显示，然后再显示 incoming surface。
   若装饰影响未审阅文本遮罩，安全遮罩必须持续有效，不能先露出正文再异步补装饰。
5. split 的像素可见性与命令归属分开；关闭标签/项目时释放监听器、计时器、索引引用和文档保留计数。
   本阶段不引入标签 LRU 淘汰；若 F0 证明保留文档本身造成内存问题，另立可恢复会话设计。

**验收 F3-01–04：** 不相关实体的 Agent 状态变化不触发此编辑器的装饰计算；隐藏标签不执行可安全延迟的显示计算；
1/5/20 标签下后台显示成本受控；远程更新、标签返回、split、保存、撤销、草稿和审阅遮罩连续正确；
反复打开/关闭后没有持续增长的监听器和文档保留数量。

**回退：** 恢复立即显示刷新，继续保留编辑会话；不得通过卸载所有隐藏标签来绕过验收。

## 8. F4：将 Agent 事件处理与界面刷新分开

**范围：** `agent-chat-store.ts`、`chat-journal-projection.ts`、transport 到投影的接线、桌面/移动聊天消费端。

1. 拆分会话路由/控制、事件去重与重放、消息投影、React 展示订阅，使 ownership 和 teardown 可独立测试。
2. 消除逐事件复制全部 `seenJournalEventIds` 的路径。内部索引可使用 Set/Map，但不能静默修改 React
   可见集合；通过受控投影版本/不可变受影响消息发布变化。
3. 区分持久 journal 的顺序/恢复游标和 transient 事件身份。去重缓存的清理必须有明确会话生命周期、
   确认范围或可恢复 checkpoint，不能任意按数量截掉仍可能重放的事件。尚无安全截断条件时，
   先消除平方复制并按会话释放，明确记录内存上限未解决。
4. 正常流式内容按帧或短时间窗口合并界面通知；底层真实事件仍按顺序完整接收和持久化。
   cancel、permission、terminal、切会话和 dispose 需要明确 flush 顺序；浏览器后台暂停 rAF 时有有界兜底。
5. 保留历史 `MessageView` 的 memo。仅当 F0 证明仍有瓶颈，再优化活动消息的增量 Markdown 和长历史视口；
   不让窗口化破坏滚动锚点、文本选择、工具卡展开状态及移动端行为。

**验收 F4-01–04：** 同一事件轨迹投影结果与参考实现相同；重复/重放/重新绑定不丢失或重复终态；
去重累计工作量随事件数近似线性，不再累计复制历史键；取消/权限响应及时，最后一段输出不会滞留；
会话清理与重新打开后的恢复正确。通知频率降低须同时证明事件完整性。

F4i 已增加真实 SQLite 文件的混合聊天进程恢复矩阵：12 个边界、2 组种子，验证 journal、工具行、
消息/checkpoint 原子提交与显示缓存之间的中断，以及独立重启后的恢复去重和消费者释放。
事务边界须由实际消息、checkpoint 和终态写入见证，不能把读取事务的提交计为回合提交。
该证据不覆盖原生 Rust 崩溃、写工具恢复执行、权限等待、checkpoint 压缩或设备性能预算；
F4-01/04 继续保持 partial，详见执行记录 F4i。
F4j 将隐藏窗口的文本显示合并到一个计时器；进入/离开隐藏状态及控制边界立即刷新，
已取消帧/计时器和上一次订阅的 visibility 回调通过代次隔离。受控浏览器场景验证
逐事件完整数组生成为 0；宿主后台计时器节流和物理设备延迟仍未验收。

F4k 将发送准备占用、失效清理和已提交回合的启动确认分别归属。每次记忆读取后检查
当前意图，旧流程不消费拒绝记录或启动模型；失效立即释放准备，旧 finally 不解锁
新发送。同项目导航保留已提交后台回合，显式取消在迟到启动确认后补发取消请求。
同一组 22 项 store 检查从基线 20 项失败变为全部通过，当前四组共 44 项检查通过，
见 `acceptance/f4-send-preparation.json`。端口为合成仓库和 transport，未证明真实
模型停止、工作记忆存储事务取消或原生/设备预算，完整 F4 验收仍未完成。

F4l 将会话列表请求、项目绑定代次和自动恢复意图归属独立控制器；旧成功/失败结果
不能覆盖较新的列表，A→B→A 也不复用第一次 A 的请求。项目切换立即清空旧列表。
手动打开会话、开始新草稿或其他显式导航会取消尚未显示的自动恢复；已经显示后的
输入仍允许权限/用户控制状态回读完成。历史消息、canonical route/journal 和计划的
读取移入独立 hydration 模块，每个异步阶段重新检查归属，store 只负责发布及命令接线。
同一 15 项 store 轨迹在基线失败 9 项，在当前全部通过；当前五组共 59 项检查通过，
见 `acceptance/f4-conversation-navigation.json`。销毁拒绝迟到列表与后续请求；普通列表
刷新不重复或取消已开始的自动恢复。合成端口验证不等同真实 SQLite/provider 恢复，
F4 完整恢复与设备预算仍待继续；异步清理命令见下述 F4m。

F4m 为删除/清空建立独立的操作与读取生命周期。SQLite UPDATE RETURNING 给出实际
删除回执，完成时只清理这些 ID，不重置新项目或较新的会话。重叠清理期间的新发送
等待完成，相关的旧读取永久失效，无关加载及普通缓存会话维持原路径；销毁释放等待者。
写入失败保留显示缓存和指针，自动继续暂停；取消请求异常不阻止耐久删除和等待释放。
已删除行不再用于会话恢复，历史用量仍计入统计，公开迁移及同步协议不变。

设置用量页按项目/统计范围绑定显示快照，切换时旧行立即退出显示；确认、删除和刷新
由独立控制器管理，使用明确项目 ID，写入成功后重新读取真实用量，失败不提前标记。
同一 11 项实际 SQLite store 检查从基线 10 项失败变为全部通过；本批共 75 项检查，
另有真实设置组件、store 和确认框的 8 项 Chromium/React Strict Mode 检查通过。
见 `acceptance/f4-conversation-removal.json`。浏览器仓库端口为合成数据，SQLite 检查
使用独立临时 WAL/FULL 文件；停止请求不证明真实 provider 已停止，既有存储事务
不会据此被宣称取消。本批没有新增性能预算结论，完整原生恢复和设备矩阵仍待验收。

F4n 将重启后取消等待的 journal 与会话/轮次/工具中断归属同一 SQLite 事务。
真实 runtime、local transport 和聊天 store 先进入权限或用户输入等待，再终止进程；
重新打开后通过实际 store 取消，在每个写入及提交前后再次终止。22 个场景、44 次
SIGKILL 与 44 次独立重启验证：未提交保留完整原等待，已提交保留完整取消，恢复
不调用模型或重跑工具。同一程序在基线复现两种等待的半提交。见
`acceptance/f4-control-recovery.json`。已有写效果的保守中断仍先单独完成；本批
不证明已开始的真实写工具恢复、原生崩溃、checkpoint 压缩或设备性能预算。
F4-01/04 保持 partial。

**回退：** 优先关闭界面批处理；可恢复原投影实现，但不删 journal、不回退 durable 决定。
不修改 Agent 协议仅为迁就 UI 批处理，也不把 transient 事件当作已持久化证据。

## 9. F5：拆分图谱计算、几何和交互

**范围：** 先做 Super Element，再将验证过的模式推广到 Story Graph、桌面/移动 Timeline。

**F5e 当前进展：** 类别卡片树和章节带已从 Super Element 主视图提取，以默认 memo
和依赖完整的稳定回调隔离无关父状态。100 / 1,000 / 5,000 元素完整 DOM 场景中，
20 次聚焦模式切换及 100 个 wheel 事件合并提交后的类别、章节带和元素 JSX 创建计数
均为 0；5,000 元素基线对应 100,000 / 5,000 次元素 JSX 创建。改名、Shift 来源
选择/取消、最新元素与章节来源配对、弹窗关闭和类别导航通过浏览器检查。加载入口使用
产品按需模块，测量从挂载稳定后开始。见 `acceptance/f5-cards-baseline.json` 与
`acceptance/f5-cards.json`；计数不等同设备延迟或首开性能。Story Graph 卡片树、完整
持久关系创建和物理手势、逐卡片数据更新与大图总体预算尚未完成，F5-01–04 保持 partial。
同源码的 `acceptance/f5-cards-native.json` 已通过新构建 Tauri 应用组合、正常退出与
SQLite/Yjs 重启恢复回归；原生大图计数、物理输入和固定设备预算仍未验收。

**F5f 当前进展：** Story Graph 行与章节卡片已独立为 `StoryGraphLaneRow`，shell 的
点击、连线、菜单、导航及拖放提交仍保有状态与命令归属。已放置节点按主视觉行一次
分组，保留原引用/顺序，副故事线统计和跨线邻接保持独立。5,000 章节 / 51 行首次
分组从 255,000 次判断降为 5,000 次；20 次未放置浮层切换或 10 次卡片浮层开关
各自原有的 100,000 次卡片 JSX 创建、510 万次分组判断均降为 0。取消拖动的卡片
重建也为 0。主故事线变更、改名、最新连线来源、右键菜单当前内容及双击导航通过。
见 `acceptance/f5-story-cards-baseline.json`、`acceptance/f5-story-cards.json`。
Story Graph 主视图从 2,182 行降至 2,055 行；剩余未放置/Drift 卡片、浮层与命令
进一步拆分、逐卡片数据更新、持久关系/拖放组合和设备性能预算仍待继续。
同源码的 `acceptance/f5-story-cards-native.json` 已通过新构建 Tauri 应用组合与正常
退出/重启恢复回归；原生大图、物理手势和固定设备预算未据此标为完成。

**F5g 当前进展：** 两图谱 Drift 卡片树已独立，闭合面板不再提前创建卡片 JSX。
Story Graph 的拖动临时来源/插入位置由卡片和特殊边层订阅，主视图只在叙事轴 drop
事件中读取最新来源并调用原有标记命令。取消、关闭/卸载和来源位置失效会清理临时状态；
视觉排序仍不写持久顺序，已去掉可能在以后数据变化时才触发的迟到 FLIP。5,000 drift
下，20 次闭合面板父操作或 10 次弹窗开关的卡片内容执行从 100,000 次降为 0；20 次
hover 的 shell 刷新为 0、内容为 19 次，包装列表仍遍历 100,000 次，未达到常数成本。
见 `acceptance/f5-drift-cards-baseline.json`、`acceptance/f5-drift-cards.json`。
双向特殊边、面板范围内的关系、最新来源/菜单、改名和关开恢复通过；Story Graph 的
取消/视觉放下、拖动中关闭清理通过。Super Element 不支持手牌排序，相关检查为 null。
未放置卡片、更多交互/命令边界、标记/关系持久提交、原生大图和设备预算仍待继续。
同源码的 `acceptance/f5-drift-cards-native.json` 已通过新构建 Tauri 应用组合、正常
退出与重启恢复；该控制组不覆盖原生 5,000 Drift 卡片或物理拖动验收。

**F5h 当前进展：** 未放置章节的按钮/锚点/浮层已由独立 memo 组件拥有，内容仅在浮层
挂载时执行；主视图保留共享 Escape 顺序、开关状态和原拖放命令。100 / 1,000 / 5,000
章节场景中，首次关闭状态不再提前创建列表；关闭/打开列表后的各 10 次章节弹窗开关，
列表内容从 2,000 / 20,000 / 100,000 次降为 0。所有章节保留，首次打开仍执行 N 次；
原书序、主故事线/未归属颜色、最新标题和颜色、拖动影子与取消、Escape、空列表和
重新打开当前数据通过。见 `acceptance/f5-unplaced-baseline.json`、`acceptance/f5-unplaced.json`。
主视图 1,898 → 1,844 行；更多浮层与命令边界、逐卡片数据更新、首次大列表挂载、
持久拖放/关系组合和设备预算仍待继续。
同源码 `acceptance/f5-unplaced-native.json` 的新构建应用组合与正常退出/重启恢复通过；
原生大列表、持久拖放和物理手势未据此标为完成。

**F5j 当前进展：** Drift 拖动显示改为按槽位订阅，列表不再随 hover 遍历。100 / 1,000 /
5,000 卡片的 20 次邻近悬停，包装 JSX 创建从 2,000 / 20,000 / 100,000 次降为 0，
内容更新仍为 19 次。双向拖动、跨越来源、来源槽位变化后的取消和节点重新插入后的
连线恢复通过浏览器检查。首次挂载仍建立 N 个订阅，长距离拖动/取消按实际影响范围
更新；不据此宣称初次挂载、整体内存、原生/物理手势或设备预算达标。见
`acceptance/f5-drift-slots.json`，F5-01–04 保持 partial。

1. 提取纯读模型：实体/类别分组、排序、关系端点与标签等；复用 F2 ID 索引。
   单次关系投影中先建 O(N) 索引，再 O(E) 查找端点，避免边循环内反复 `.find()`。
2. 视图分为稳定卡片树、连线/几何层、视口控制和 shell 交互/浮层。让几何 state 属于 overlay，
   不因一条线的位置变化使整棵卡片树重新执行。
3. 每次必要测量中，对同一个 DOM 端点至多读取一次布局；scroll/resize 合并到帧，先集中读再集中写。
   几何结果相同则不 setState。入口动画结束后停止测量，遵循现有有限动画窗口。
4. 保留当前平移的 ref/DOM transform 和交互结束提交模式；按需使连线层跟随或重算。
   不把原本已避免的逐帧 React 更新重新引入控制器。
5. 再根据大图证据决定可见区域裁剪：计入选中、拖动、键盘焦点、跨视口边和 popover 锚点。
   从纯读模型/世界坐标确定端点，不能裁掉 DOM 后让边断开。首轮不整体迁移 Canvas/WebGL。
6. 共享计算，不统一 desktop pointer 和 mobile gesture 的所有策略。popover 继续 portal 到 body 并使用 fixed。

**验收 F5-01–04：** 几何刷新不重复渲染未变化卡片；每帧端点测量受唯一端点数约束；大图投影复杂度可验证；
平移、缩放、拖动、关系创建、特殊 drift 边、键盘导航和移动触摸保持正确；裁剪若实现，边/选区/浮层无缺失。

**回退：** 按图谱逐个启用/回退；视口裁剪和新几何调度可分开撤销，保持同一领域关系模型。

## 10. F6：工作区投影和引用索引按变化更新

**范围：** `ProjectRuntimeProvider`、`workspace-projection.service.ts`、`reference-index.service.ts` 及提交后的失效通知。
这是最高数据风险阶段，内部必须分两步交付。

**F6a：先控制派生计算，保持完整一致性读取。**

1. 保留原子捕获和发布；在一致快照内识别未变化记录/集合，复用引用。
   比较成本需要测量，不能通过多次 stringify 全库制造新的热路径。
2. 建立明确的变更影响表：纯正文、指标、元数据、关系、删除、恢复分别影响哪些派生结果。
   引用索引按正文来源处理；只变外观且没有引用语义变化时不重解析全库正文。
3. 将无归属的整项目异步重建改为项目级、可合并、可取消/作废结果的索引队列。
   队列只响应 durable commit 后的变化，检查源版本、project 和 generation；失败必须可观察并重试。

变更影响表（前两列已核对现有调用路径；最后一列为后续引用队列的目标契约，尚未全部实现）：

| 变化 | 工作区/显示影响 | 引用派生目标 |
| --- | --- | --- |
| 纯正文提交、远程 Yjs 更新 | 正文走现有 Yjs 通道；节点指标单独协调，不阻塞结构视图 | 仅处理受影响来源的已提交正文版本；不能用旧 contentJson 覆盖较新正文引用 |
| 字数、指标依据更新 | 对应节点记录改变；名称和外观索引仍按原语义失效 | 无需重新解析正文 |
| 名称、别名、颜色、位置等元数据 | 对应记录和相关名称/外观/布局消费者更新 | 更新引用面板所需标题/显示；已有 mark 的目标 ID 不因外观变化而全库重解析 |
| 人工关系及关系类型变化 | 关系集合、计数和过滤视图更新 | 按关系查询路径更新，避免重解析无关来源正文 |
| 删除、恢复、来源/目标生命周期变化 | 实体、成员关系及反向映射须原子一致 | 清理已删除来源的派生行，按现有 dangling/恢复规则处理目标；验证迟到任务不能复活已清理行 |
| checkpoint restore、generation 切换、未知影响 | 保留完整捕获与保护屏障 | 作废旧任务；权威全量重建和覆盖校验仍是显式恢复入口 |

F6a1 完成引用复用和成员表计算；在该批次结束时，引用重建与编辑器写入尚未经过队列，
其验收不能证明正文来源增量或崩溃恢复。

F6a2 增加独立的 `reference-index-repository` 事务边界：在同一 SQLite 事务捕获来源与
持久 Yjs 版本，正文存在 Yjs 时忽略 `contentJson` 缓存；替换引用前再次检查数据库 owner、
项目、generation、来源存在性及版本。过期结果拒绝，删除/插入失败回滚。来源清理也在写事务
内重读存在性。28 项合成文件 SQLite 测试通过，结果见
[`f6-reference-boundary.json`](acceptance/f6-reference-boundary.json)。该批次结束时尚未替换运行中的
引用重建与编辑器写入。F6a3 已完成下述接线；F6b1 的进程强杀验收见下文。

F6a3 将启动重建和编辑器直接投影统一迁入项目队列。普通/Agent 本地提交、远程正文和结构提交
都在耐久边界后触发；250 ms 固定窗口合并通知，处理中再有变化只保留一个后续请求。每 16 个
来源让出主线程。失败独立按 1–30 秒退避重试，成功来源不重复解析；错误通过引用区域显示。
恢复、authority 切换、显式重试和新 owner 全量核对。版本和覆盖行数均一致时才复用缓存，避免
实体生命周期清理引用后仍误判完整。Agent 外层事务通过新增提交后回调接入，保存点/外层回滚不通知。

[`f6-reference-queue.json`](acceptance/f6-reference-queue.json) 记录 SQLite 队列测试和隔离浏览器
中的真实状态组件验收：100 个来源仅改元数据时解析/写入 0 个，单正文变化为 1 个；100 次健康
状态通知为 0 次额外 React commit。**F6a3 结束时，每次队列执行仍完整读取来源目录、Yjs 版本及引用覆盖计数，
包括纯正文提交。** 与之前编辑器直接解析 live doc 相比，这会增加目录读取；不能把解析次数下降
解释为数据库或整体输入延迟已经改善。F6b 必须测量读取量和时延，并在恢复证明后收窄正文提交的读取。

**F6b：证明恢复后，再减少数据库读取范围。**

F6b1 使用独立 Node 进程、真实 SQLite 文件和完整产品 schema，首次执行 46 次 SIGKILL 及
92 次独立重启，覆盖 Yjs 正文、JSON 补丁更新和补丁删除。中断位置包含作者事务提交前/后、
队列等待、目录捕获、索引删除/插入/提交和队列确认；删除场景没有插入步骤，因此不伪造该位置。
重启走真实项目 service 的全量入口，结果同时对照独立 span 预期和无缓存完整重建；
检查正文/同步日志等权威表 hash、另一个项目的引用、Yjs revision、SQLite 完整性与外键。
生成报告为 `acceptance/f6-reference-recovery.json`。原生安装身份使用合成替身，其他作者命令、
事务、journal、队列和仓库均为现有实现；SQLite 通过 Node 网关适配器运行，原生 Rust/设备和
断电恢复未验收，F6-04 仍为 partial。该证据允许继续收窄 renderer 数据读取；不能替代总体性能验收。

F6b2 在普通作者、Agent 和远程的纯 Yjs 提交后传递有界来源提示，仅作 renderer 调度信息，
不改变 SyncEngine wire contract。已建立完整覆盖的队列合并最多 128 个来源，通过同一事务按
来源 ID 批量读取正文元数据、Yjs 版本/存在性和引用计数，避免全目录及逐来源元数据查询。
版本校验和替换事务共用完整路径的实现；存在性检查不读取 snapshot blob。启动、恢复、
同步运行时重建、未知/混合/过大范围、失败及陈旧结果仍完整核对；来源缺失和 generation
变化先放弃局部结果，再全量清理。结构和 JSON 补丁提交仍使用完整捕获。

远程已提交后若 live Yjs 合并或产品通知失败，下一轮在 session 恢复后补发同一范围；
新建 sync runtime 在首次 apply 单独使引用覆盖失效一次，避免丢失内存中的未送达通知。
纯正文的非阻塞通道和结构更新保护屏障不变。运行中的局部队列强杀场景使恢复矩阵扩展到
62 次 SIGKILL / 124 次独立重启，仍不等同于原生设备或断电验收。

`acceptance/f6-reference-reads.json` 记录 97 项用例、独立运行的 64/1,024 来源 SQL 读取对照、
以及隔离浏览器状态组件检查。读取对照比较当前全量回退与局部捕获，每个模式先预热一次，
再交替运行五次；分别记录 query 数、返回行/序列化字节、SQLite 网关耗时、解析/写入和发布。
总 pass 时间包含观测数据序列化开销，只作诊断；不据此宣称原生输入延迟达标。
结构 workspace 的依赖闭包读取、完整 App 性能与设备验收仍待继续。

**F6b3：结构刷新队列与并发发布保护。**

`workspace-projection-refresh.ts` 从项目 Provider 中接管结构刷新，保留项目级所有权和保护屏障。
固定首个事件起算的 250 ms 窗口，不再被连续事件无限延后；只允许一个进行中读取和一个待处理
请求。发布同时核对数据库、项目、epoch、读取前全部数据分片的引用和项目元数据；本地编辑或
派生指标在读取期间推进时，拒绝旧结果并以 500 ms–4 s 退避重读。旧 missing/error 结果不能
清空或阻塞新请求。恢复与 authority 切换请求完整刷新，纯正文仍只触发后台指标核对。
`acceptance/f6-workspace-refresh.json` 记录真实 SQLite 队列和 store/wiring 的 28 项正确性用例；
虚拟时钟不代表性能测量，未挂载完整 App，未执行原生/设备验收。

F6b3 结束时，结构局部读取尚未实现：远程生产 materializer 会重新物化启用的历史赢家，
因此“本次 mutations 的实体 ID”并不等于实际依赖闭包。F6b4 用以下独立失效记录覆盖实际写入，
保留既有 materializer 语义。

**F6b4：记录实际 SQLite 变化，按覆盖范围读取。**

追加 `0002_workspace_projection_journal.sql`，不改动 0000/0001。17 个领域/依赖表的触发器
在同一事务记录实际变更，包括历史赢家重新物化、级联和派生指标；本地失效表不进入同步、
checkpoint 作者域或通用 CLI 写入。每项目最多保留 4,096 次修订范围的实体标识，记录压缩
下界；跨越下界、generation 更换、重建、未知范围或缺失覆盖时使用完整读取。

队列只复用同一数据库上最后一次接受的权威捕获。节点元数据变更在 128 个 ID 内时只读取
受影响行；身份替换、可见性、kind、排序变化和大批次提升为完整节点集合。删除后同 ID 重建
有独立 replacement revision，不能凭相同时间戳复用旧顺序。其他受影响集合仍完整读取，
并补齐节点/故事线的有序归属依赖。所有数据在同一事务捕获，再接受既有并发发布保护。
丢失通知会在下一次捕获时补齐；没有后续事件时不承诺立即刷新。首次刷新仍完整捕获。

验收入口：`pnpm workspace:refresh:acceptance` 生成集合/行范围、写入成本与原生迁移报告；
`pnpm workspace:projection:recovery` 生成作者事务与实际刷新队列的独立进程恢复报告。
结果见 `acceptance/f6-workspace-reads.json` 和 `acceptance/f6-workspace-recovery.json`。
Node SQLite 的读取/发布计时、原生迁移安全快照和完整 App/设备性能分别记录，不能相互替代。
F6 保持 `in_progress`；其他集合逐行读取、原生 renderer 恢复及整体性能门槛仍待后续验收。

**F6b5：元素正文按受影响行读取，排序单独读取 ID。**

在已有日志覆盖内、最多 128 个仍存活的既有元素变更时，只读取变更元素的完整行；
全体存活元素的 ID 由 SQLite 按 updatedAt 降序、同时间戳 rowid 升序返回，
与完整/分类查询使用相同排序，避免在 JavaScript 中猜测并列顺序。专用读取模块负责
合并和回退，保留其他元素的对象引用及未变化的回收站集合。分类删除等实际依赖变化
仍在同一事务捕获，发布继续遵守本地编辑的引用屏障。新增、删除、可见性变化、
身份替换、大批次和覆盖不足仍走完整集合或完整工作区，不追加或改写迁移。

`pnpm workspace:elements:acceptance` 用同一测试文件在旧提交和候选代码各执行真实
SQLite 捕获，生成 `acceptance/f6-element-reads.json`：55 项检查通过，
65/1,025 个元素中修改一个时，读取完整元素行从 65/1,025 降为 1，查询仍为 5 次。
仍需 O(N) 的 ID 读取、SQLite 排序和数组合并；不能宣称整体读取或 React 成本为常数。
重新执行的 `workspace:projection:recovery` 在元素局部捕获边界验证实际采用 changed
路径后再 SIGKILL，20 次终止和 40 次重启通过。Node 数据传输与进程恢复证据不替代
原生 renderer、物理输入、断电恢复或完整 App 延迟验收，F6 保持 `in_progress`。

4. 定义引用覆盖进度或可重建失效来源，保证进程在提交后、排队前或索引写入中退出，都能在启动后补齐。
   纯内存 dirty Set 不足以证明恢复完整。优先使用已有耐久日志/版本；若需新表，单独审核新迁移。
5. 对确定影响范围的变更，计算实体及关系依赖闭包，在一致性事务中读取受影响数据，再原子发布。
   防止晚到的旧局部结果覆盖较新的全量刷新；不能逐表直接 set 到 UI。
6. bootstrap、checkpoint restore、修复、未知影响和覆盖不可信时仍走显式全量重建。
   全量/增量共用正确性校验，不长期维护两套含义不同的数据投影；队列需要追赶进度和背压，不能无限积压。

**验收 F6-01–05：** 单来源正文变更仅更新受影响引用，删除清除对应引用；语义无关结构变化不全库解析正文；
纯正文同步继续走既有增量通道；结构依赖原子一致；项目切换和乱序异步结果无法污染新状态；
在提交/排队/索引写入边界强制终止后重启，结果与从权威内容完整重建一致。
需分别统计 SQLite 读取、React 发布、引用解析成本，不能只证明其中一个下降。

**回退：** 切回全量读取/重建，继续遵守 epoch 屏障；所有派生缓存可重建。
已发布迁移只能向前修复，不回写旧迁移或降级数据库，不删除作者数据以消除差异。

## 11. F7：按访问需求加载重型功能

F7a 的生产入口加载基线（提交 `83afe28`）见 `acceptance/f7-libraries-baseline.json`：未使用功能时，PDF 引擎和
JSZip 均已执行并进入解析脚本。首屏静态 JS 依赖闭包为 5,777,492 字节；这是隔离浏览器
中的模块加载证据，不是原生冷启动耗时或整 App 就绪验收。

F7a 已将 PDF 引擎/本地 worker 配置收拢到异步运行时，预览和缩略图共享模块加载；JSZip
只在实际构建导出时加载。`pdf-preview-document.ts` 接管单个预览的读取/加载/销毁生命周期，
关闭后不创建迟到的 PDF 任务，解析失败也释放任务。新报告 `acceptance/f7-libraries-deferred.json`
记录 43 项资源/平台/导出用例、浏览器实际模块执行与解析、真实 PDF 像素结果和 ZIP 解压。
静态入口 JS 依赖为 5,188,855 字节；两个目标库未使用时均未执行、未解析，首次功能访问才加载。
这些构建包含验收观察代码，首开 PDF 的单次计时仅作诊断，不代表 F0 延迟预算已通过。
F7a 结束时，其他入口、chunk 获取失败重试、原生离线资源与完整 App 启动均保留为待办；设置入口的后续进展见 F7b。

F7b 已保留三个设置入口的导航外壳，将六组设置内容统一放入延迟入口；移动回收站的小面板独立，
不再串入整套偏好设置。`acceptance/f7-settings-baseline.json` 从隔离的 `4a6734d` 检出构建，
`acceptance/f7-settings-deferred.json` 记录同样的生产入口观察：初始 JS 依赖从 5,187,486 降至
5,012,890 字节，六组面板均未在未访问时执行或解析。浏览器对失败的 import 保留错误，因此
设置加载器为同一构建文件的显式重试增加新的查询标识；真实阻断请求后，桌面/移动独立设置均恢复。
加载期间栏目选择、返回及桌面弹窗 Escape 保持可用；重复访问复用成功的代码缓存。
这批没有验证完整项目设置、编辑器/ProjectRuntime 组合连续性、原生资源协议或 F0 延迟预算。

F7c 将故事/元素图谱分别迁入延迟入口，先加载可独立重试的共享图形组件，再加载选中的视图主体。
桌面 host 与移动兼容入口共用局部 loading/error 外壳，导航和返回保持可用。共享代码成功后复用，
不缓存项目、编辑器或图谱实例。生产图检查覆盖静态 JS/CSS 依赖，防止冷共享依赖绕过重试所有者。
`acceptance/f7-graphs-baseline.json` 从干净的 `d7e8c9a` 检出构建；对照
`acceptance/f7-graphs-deferred.json`，初始静态 JS 依赖从 5,013,055 降至 4,923,305 字节。
两个视图及共享图形模块在未访问时均未执行、未解析。真实浏览器逐一阻断三个请求并重试，
验证加载中切换视图、Tiptap/Yjs 合成草稿连续性、切换合成项目后的迟到结果隔离、焦点转移和缓存再开。
设置加载回归另存 `acceptance/f7-settings-after-graphs.json`，历史 F7b 报告保持不变。
这批不等同于完整 ChapterEditor/ProjectRuntime、原生平台交互或 F0 延迟预算验收；预加载仍待继续。

F7d 增加入口意图预加载：停留 120 ms 后在 idle 窗口请求，最多一个后台任务和一个待处理意图；
新的待处理意图替换旧项。离开入口、取消触控、卸载、窗口失焦或页面隐藏取消未启动任务。
失败的后台尝试不污染首次打开；正式点击可接管同一请求，也可绕过其他后台任务直接加载。
图谱主入口、图谱切换/移动结构入口及设置入口共用已有代码缓存，后台不创建视图或项目实例。
每个资源每次 renderer 生命周期最多尝试一次后台预加载，失败不会因重复 hover 反复请求。
`acceptance/f7-intent-preloading.json` 记录真实入口浏览器检查、既有图谱重试回归和确定性调度测试。
完整项目/设备连续性与首开预算仍未计为通过；F7 与 F8 的其余门槛继续保持未完成。

**范围：** `AppRoutes.tsx`、desktop/mobile shell 入口、`DesktopOverlayHost`、图谱和低频模态框、Vite 构建分析。

1. 记录首屏真正加载、解析、执行的模块，而不只观察打包后的 chunk 总数。
2. 按路由/功能入口引入 dynamic import，优先低频设置、重型图谱和独立模态框；检查 barrel 静态导入是否重新串回主包。
   项目运行时和必要原生事件监听须按已有启动时序就绪，不能为了缩小首包延后到丢失事件。
3. 预加载基于用户入口或首次空闲，限制并发；首次加载保留稳定外壳和可取消状态，后续访问复用模块缓存。
   incoming editor 的加载继续遵守既有 ready 屏障，不用 Suspense fallback 覆盖正在写作的整个工作区。
4. 为 chunk 加载失败提供局部错误与重试；验证原生本地资源路径、离线启动、升级后的资源一致性。
   对 desktop/mobile 分别测量，不将 native 本地 JS 的 gzip 缩小等同于网络收益。

**验收 F7-01–04：** 不访问重型功能时不加载/执行对应代码；访问后功能完整且重复打开无空白闪烁；
模块失败不会丢失编辑状态或重启项目运行时；首屏和首开功能均达到 F0 预算，避免将全部耗时转移到首次点击。

**回退：** 单个入口可恢复 eager import；不调整作者数据、导航含义或 runtime 生命周期。

## 12. F8：组合验收与防止回退

1. 在基线和最终实现执行同一批 fixture/操作序列，报告输入、标签、Agent、图谱、同步、启动和内存的变化。
   列出退化项及原因；局部计数下降不能代替整体效果。
2. 覆盖中文 IME、长文粘贴、连续撤销、快速标签/split 切换、隐藏标签接收远程正文、Agent 输出并行编辑、
   结构同步、项目切换、取消、断开/重连和进程恢复。压力轨迹只用合成数据。
3. 将确定性约束纳入普通 CI：无关订阅通知、失效范围、事件等价性、重复处理、队列恢复、项目隔离、依赖边界。
   受设备噪声影响的 p95/内存/原生启动放到固定性能环境；结果带容差与复跑规则，不让随机时延拖垮普通测试。
4. 桌面原生新构建、Android/iOS 可用环境分别记录。模拟器、合成输入、物理设备、真实账号同步验收状态分开；
   缺失设备保持 `NOT RUN`，不沿用旧二进制的结果。不会用本计划代替已有签名 RC 或 Drive 实机门槛。
5. 更新实际已实现的 renderer 契约、编辑生命周期、同步/Agent 文档，以及对应生成器产生的验收证据。
   计划文件只链接真实输出，不手工把生成文件改成“通过”。未过门槛的阶段不得标 complete。

**验收 F8-01–04：** 组合场景达到固定预算和正确性门槛；CI 回归保护可复现；文档与精确 SHA 对齐；
实机和发布缺口明确，未测项目没有被计入已验收。

**回退：** 按阶段定位并撤回引起退化的变更；先恢复可靠数据与显示，再重新测量。
没有证据收益的复杂抽象应简化或撤销，不为保留重构成果追加永久兼容层。

## 13. 检查、证据与执行边界

各阶段先运行针对实际风险的行为测试，合并前执行仓库规定的检查：

```bash
pnpm ci:contract:check
pnpm public:check
pnpm lint
pnpm typecheck
pnpm test
pnpm agent:capabilities:check
pnpm test:renderer-architecture
```

构建使用 CI 的明确环境配置：

```bash
VITE_LOCAL_ONLY_MODE=true VITE_REQUIRE_AUTH=false VITE_AI_TRANSPORT=direct \
VITE_API_BASE_URL=http://localhost:3000 API_BASE_URL=http://localhost:3000 \
pnpm exec vite build --config vite.renderer.config.ts
```

涉及 Agent 会话同步受保护源码时，运行 `pnpm agent:conversation-sync:check`；若生成证据需要更新，
使用 `pnpm agent:conversation-sync:evidence` 真实运行生成器，然后检查 diff。涉及调试入口时执行
`pnpm frontend:debug:check`。原生迁移/恢复、触摸与性能须补对应专项验证，不能以 TypeScript 测试替代。
纯计划文档变更无需重新生成实现证据。

F0 应补充一个独立计划/报告校验入口，校验阶段依赖无环、验收 ID 唯一、证据路径存在及结果归属。
每份性能运行报告应由执行器输出 fixture、build、scenario、metric、unit、samples、summary、status 和
局限说明；只存合成操作统计及脱敏环境信息。计划 JSON 中的 evidence 只引用已发生的运行结果。
机器通过与人工/实机验收分开表示，CI 不得将单纯填写文档视为完成。

建议按三批评估是否继续投入：

- **第一批 F0–F3：** 达成输入与多标签场景的首轮可测收益；完成后再决定大范围 UI 拆分的必要性。
- **第二批 F4–F6：** 处理长会话、大图和大型项目同步的规模增长；F6 单独保留恢复验收关口。
- **第三批 F7–F8：** 优化加载，验证组合效果并固化防回退规则。

实施进度和实际证据统一记录在[执行记录](README.md)及验收清单；未完成的项目不得记为达标。

## 14. 实施时的源码入口

以下路径均相对仓库根目录，供定位；阶段开始时应重新检查源码与基线差异。

- 编辑器：`src/renderer/hooks/useEntityEditor.ts`、`src/renderer/lib/extensions/entity-link.ts`、
  `src/renderer/lib/entity-link-appearance.ts`、`src/renderer/components/editor/EditorMainArea.tsx`。
- 数据：`src/renderer/store/data-store.ts`、`src/renderer/views/NodeEditorView.tsx`、
  `src/renderer/services/node-prose-metrics.service.ts`。
- Agent：`src/renderer/store/agent-chat-store.ts`、`src/renderer/lib/agent/runtime/chat-journal-projection.ts`、
  `src/renderer/lib/agent/runtime/local-transport.ts`、`src/renderer/features/agent/AgentMessageViews.tsx`。
- 图谱：`src/renderer/shells/desktop/views/DesktopSuperElementView.tsx`、`DesktopStoryGraphView.tsx`、
  `DesktopBottomTimeline.tsx`，以及 `src/renderer/shells/mobile/workspace/timeline/MobileVerticalTimeline.tsx`。
- 投影：`src/renderer/app/providers/ProjectRuntimeProvider.tsx`、`src/renderer/services/workspace-projection.service.ts`、
  `src/renderer/services/reference-index.service.ts`。
- 加载与边界：`src/renderer/app/AppRoutes.tsx`、`src/renderer/shells/desktop/DesktopOverlayHost.tsx`、
  `vite.renderer.config.ts`、`src/renderer/architecture/renderer-boundaries.test.ts`。
